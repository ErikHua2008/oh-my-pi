# Grimoire cosmic cube — blue backup

This directory is an exact backup of the filled blue cosmic-cube brand assets
that preceded the monochrome line-art production variant. It includes the
light and dark masters, all favicon sizes, Windows ICO files, and the derived
Tauri icon set.

The live application assets remain under `packages/collab-web/public` and
`packages/tauri-shell/icons`.
